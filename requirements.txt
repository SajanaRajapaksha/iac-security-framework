# =============================================================================
# FILE: requirements.txt
# PROJECT: iac-security-framework
# PURPOSE: Python dependencies for the IaC security framework scripts
# =============================================================================
#
# PHASE 1 (Core):
#   Python Standard Library Modules Used (no installation required):
#   - uuid, hashlib, json, os, pathlib, datetime, argparse, sys
#
# PHASE 2 (Risk Scoring Engine):
#   External dependencies for CIS-driven AI-assisted risk scoring.
#
# =============================================================================

# ---- Risk Scoring Engine dependencies ----
openai>=1.30.0
pyyaml>=6.0
jsonschema>=4.20.0

# ---- AWS SDK — required for deployment resource discovery and verification ----
boto3>=1.34.0

# ---- Runtime Security Scanning ----
prowler==3.12.1

# =============================================================================
# TOOL DEPENDENCIES (installed separately — not via pip):
# =============================================================================
#
#   Terraform CLI   — via hashicorp/setup-terraform GitHub Action
#   Checkov         — via: pip install checkov
#   Conftest        — via binary download
#   Trivy           — via apt/binary download
#   OPA CLI         — via binary download (optional — Conftest wraps OPA)
#
# =============================================================================
